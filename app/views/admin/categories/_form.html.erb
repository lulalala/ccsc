<%= simple_form_for [:admin, @category] do |f| %>
  <%= f.input :name %>
  <%= f.input :scope %>
  <%= f.association :parent, collection: Category.where(parent_id: nil) %>
  <%= f.submit %>
<% end %>
