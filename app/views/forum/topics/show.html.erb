<% comment_topic = @topic.comment_topic %>

<%= link_to "⏎ 回到 #{@board.name} 列表", admin_forum_board_path(@board) %>

<h1>
  <%= @topic.title %>
</h1>

<div class="comment">
  <% if @topic.comment_topic.present? %>
      <%= cell 'comment/topic', :show, @topic.comment_topic %>
  <% end %>

  <hr/>

  <%= cell 'comment/owner', :form, @topic %>
</div>
